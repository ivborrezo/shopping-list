/**
 * Raíz del microservicio {@code product-service}.
 *
 * <p>Contiene la clase de arranque y, como subpaquetes, los paquetes de dominio ({@code category},
 * {@code product}) y los componentes transversales ({@code config}, {@code common}).
 */
package dev.ivborrezo.shoppinglist.product.service;
